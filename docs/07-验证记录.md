# v1.1 交付验证记录

日期：2026-09-22。范围为文档与离线设计预览，不是 Android 功能验收。

## 已完成检查

- Chrome 无头浏览器检查通过：默认无可见文字与控件；12 状态切换；D/Esc 面板；右上角长按 2 秒；模拟唤醒同步开启会话与相机状态；模拟休息取消后续定时回调；竖屏不溢出。
- 预览无浏览器脚本错误、无 HTTP 网络请求，源码没有录音、摄像头或模型调用。说话动画为合成动效；“模拟相机开启”没有实际打开相机。
- 三份 Word 已生成、渲染并检查全部页面：PRD 5 页、技术 5 页、UI 9 页，共 19 页。UI 内含 12 张表情画面。表格无残缺单元格，无空白页；检查了中文显示、页面边界、图片及分页。修正了初版图片挤出下一页和单段孤立成页的问题。
- 采购 Excel 与 v1.0 的 SHA-256 相同，保留原有工作簿及此前公式验证结果；本轮没有修改预算或声称重新询价。
- 用户确认的 30 秒连续等待、10 分钟会话上限已同步。视觉失败、取消、长媒体关闭相机与完全静音规则写入文档。

## 实际运行的主要命令

在项目根目录执行（以下个人安装路径已替换为通用命令/变量；这是历史检查记录）：

```sh
node .artifacts/android-robot-v2/verify_preview.cjs
python3 .artifacts/android-robot-v2/build_documents.py
python3 .artifacts/android-robot-v2/verify_delivery.py
```

以上最终退出码均为 0。浏览器报告见 `.artifacts/android-robot-v2/preview-validation.json`，文档检查报告见同目录 `delivery-validation.json`。

Word 渲染使用以下命令，PRD 与技术文档采用相同入口并替换输入文件和输出目录：

```sh
FONTCONFIG_FILE=.artifacts/robot-design/fonts.conf python3 "$DOCUMENTS_RENDERER" outputs/android-robot-20260922/03-Android机器人App与UI交互方案.docx --output_dir .artifacts/android-robot-v2/render-ui-delivery --emit_pdf
```

最终三份渲染均退出 0。渲染 PDF 和页面 PNG 留在内部校验目录，交付包提供 Word 原稿。

环境适配中，Python Playwright 不存在、Playwright 默认下载的 Chromium 不存在；改用 bundled Node Playwright 和系统 Google Chrome 完成检查。Python 的 fitz 不存在，改用已安装 pypdf 检查 PDF。没有把这些失败尝试报告为通过，也未为此安装额外依赖。

## 尚未完成

没有构建或安装 APK，没有真实音频、唤醒词、连续对话、AEC 双讲、视觉模型或摄像头权限接入。HTML 的短流程演示不是 30 秒或 10 分钟真机计时验收。手机实际功耗、温度、延迟、误唤醒和视觉准确率，需要在选定手机、昵称与模型后执行 PRD 验收。系统级锁屏唤醒不在本版保证范围。

## v1.2 双端身份补充检查

本次只新增双端身份与配对设计、更新阅读指南和打包，不改变 v1.1 Word、产品技术 UI 原稿或动态预览。检查同一个 APK 的两种身份、PIN 入口、服务器管理通道、绑定与蓝牙的区别及未实现边界，并逐字节比对保留的 v1.1 文件。执行命令：

```sh
python3 .artifacts/android-robot-v2/verify_v12.py
```

本检查只验证文档条款存在、基线保留和 ZIP 完整性，不代表已经测试账号权限、配对协议或两部真机的通信。结果记录在 `.artifacts/android-robot-v2/v12-validation.json`。

## 目录清理后的验证

按用户要求删除旧日期交付、v1.1 压缩包、旧设计草稿及临时渲染结果。基础方案与 v1.2 补充共同保留。清理前记录文件 SHA-256，清理后检查最新原稿、Word、采购表、表情预览及原始调研均未变化。清理范围和结果见 `.artifacts/android-robot-v2/cleanup-validation.json`。

上文命令属于当时的执行记录：旧字体配置已迁移至 `.artifacts/android-robot-v2/fonts.conf`，PDF 和页面截图已删除但可重新生成。原有渲染检查结果继续保存在 `delivery-validation.json`；当前 `verify_delivery.py` 在没有渲染文件时只检查 Word 和交付结构，不声称重新完成页面渲染。

清理后重新执行 `verify_delivery.py` 和 `verify_v12.py`：前者检查现有 Word、采购表哈希和已确认规则；后者检查保留文件哈希并重新打包唯一的 v1.2 资料包。当前结构检查记录在 `current-delivery-validation.json`，压缩包完整性结果在 `v12-validation.json`。

## 迁入 docs 后的验证

文档、Word、采购表、表情图片、HTML 与资料包已从原交付目录统一迁到 `docs/`，原 `outputs/` 空目录已移除。上文旧目录命令保留为历史记录，不代表当前可直接执行的路径；生成和校验脚本均已改用 `docs/`。

迁移时逐项比较 SHA-256，确认移动没有改动文件内容。两份调研保留资料来源与许可证核查，移除旧版点击说话、字幕和入口设计建议。阅读指南、项目入口、生成脚本、校验脚本、当前构建路径及哈希清单均同步更新；资料包新增调研子目录。

使用 `verify_delivery.py`、`verify_v12.py` 重新校验当前文档与压缩包；另执行生成脚本语法检查、README 本地链接检查及 HTML 浏览器加载检查。未重新生成 Word 或执行手机功能测试。迁移记录见 `.artifacts/android-robot-v2/migration-validation.json`。

## v1.3 协作说明与产品细节讨论稿验证

2026-09-22 新增 09 内容播放/声音/三端协作说明、10 整体产品细节与优先级，更新 README 和阅读指南，区分已确认需求与推荐方案。核对媒体下载、系统 TTS 和 sherpa-onnx TTS 官方资料，引用见 09。

实际执行命令（工作目录为项目根目录）：

```sh
python3 .artifacts/android-robot-v2/verify_v13_docs.py
```

退出码 0，结果 PASS：检查 13 个 Markdown 文件的本地链接与代码块闭合、两份新增文档的入口与讨论稿标记，并核对 14 个保留基线文件的 SHA-256 未变化。结果在 `.artifacts/android-robot-v2/v13-docs-validation.json`。另人工检查已确认的 30 秒/10 分钟、相机联动和纯表情规则，以及新增媒体、时间限制、离线和配置确认流程的衔接。

本轮未构建 APK、运行模型、试听音色、导入/播放真实内容或测试手机禁用时段，也未重建 Word、HTML、Excel 或 ZIP。v1.2 压缩包为历史快照，不含本轮讨论稿。`verify_v12.py` 为历史打包脚本，本轮未调用；不要据其版本名认为它会生成 v1.3 资料包。

## v1.4 资源库与图书设计验证

2026-09-22 更新产品第 9 节、技术第 10 节及对应两份 Word，新增 11 资源库与图书维护指南，同步 README、阅读指南和 v1.3 文档的版本说明。补充纸书转文字、按页校对、审核发布、封面选书、原文朗读、版本进度、其他资源管理及备份恢复。功能仍为设计，未接入实际 OCR、TTS 或资源管理服务。

实际执行命令（项目根目录）：

```sh
python3 .artifacts/android-robot-v14/build_docs.py
python3 .artifacts/android-robot-v14/build_docs.py --only 01
FONTCONFIG_FILE=.artifacts/android-robot-v2/fonts.conf python3 "$DOCUMENTS_RENDERER" docs/01-Android机器人产品需求文档.docx --output_dir .artifacts/android-robot-v14/render-prd --emit_pdf
FONTCONFIG_FILE=.artifacts/android-robot-v2/fonts.conf python3 "$DOCUMENTS_RENDERER" docs/02-Android机器人技术方案.docx --output_dir .artifacts/android-robot-v14/render-tech --emit_pdf
python3 .artifacts/android-robot-v14/verify_docs.py
```

均退出 0。产品 Word 最终 8 页、技术 Word 11 页，逐页查看渲染图片，正文和表格可读，无截断或空白页。产品修订排版后重新渲染检查，并移除过期的第 9 页校验图片。Word 与 Markdown 按顺序比对正文及表格单元格，分别 127、165 个文本块一致。校验覆盖 14 个 Markdown 的代码块闭合和 23 个本地链接，R01—R26 唯一连续；11 个未改动资料的 SHA-256 与本轮前基线一致。

结果见 `.artifacts/android-robot-v14/validation.json`。本轮未执行 APK 构建、真实图书导入、封面识别准确率、朗读试听、设备离线或时段控制测试；PRD 样书验收数字为待执行目标。UI 文档/预览、采购表和历史 v1.2 ZIP 保持原样。旧版校验脚本与哈希清单保留为历史记录，本轮验证使用 v1.4 脚本。
