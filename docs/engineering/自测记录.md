# 开发自测记录（持续更新）

## 2026-09-24 V8 合并后的提交检查

V8 已快进合入 main（d33b3cd），原目录的安静回答修复、长测配置恢复、声学诊断、观察器与 CI 修改已恢复并共同验证。本次提交收录这些原有修改；不把历史设备证据重新标记为新版本全量真机验收。

实际执行 `PYTHONPATH=ai-service ai-service/.venv/bin/python -m pytest ai-service/tests scripts/tests -q`：117 passed、2 个第三方弃用警告；`ai-service/.venv/bin/python -m ruff check ai-service/robot_service ai-service/tests scripts` 通过。JDK 17 下执行 `./android-app/gradlew -p android-app :core:test :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug --offline --max-workers=1`：BUILD SUCCESSFUL in 56s，核心测试报告 35 项、0 失败、0 错误。合并日志保存在本地 `.artifacts/integration-v8/build.log`。

本轮 V8 真机范围见 [V8 真机验收](../design/V8真机验收-2026-09-24.md)。下方 2026-09-23 的模型、声学与长测条目均为对应版本的历史记录；4 小时/24 小时长测、真实儿童声学及家庭材料等缺项仍未据此通过。

## 2026-09-23 验收工具纳入持续检查（当前）

统一检查脚本和GitHub Actions已加入 `scripts/tests`，Android job追加测试APK编译，避免长测观察器和真机测试源码脱离检查。实际 `pytest ai-service/tests scripts/tests -q` **109项通过，2.87秒**、2条既有弃用提示；`ruff check ai-service/robot_service ai-service/tests scripts`、`bash -n scripts/check.sh`、`git diff --check`通过。测试APK最近实际构建及华为证据见下；本轮没有重复运行无关Android构建，也没有远端CI运行或发布。

当前仍56/61：8.5需要真实家庭10本书及人工听读；8.6需要目标儿童与可确认现场声学；8.7正式混合失败、待机未启动，不能把受控短诊断替代长测；8.8及9.5依赖这些证据。矩阵A07系统重启/时钟组合应在家长可解锁可恢复现场执行；A09双手机家庭网络缺已授权第二台测试设备。发布签名、仓库维护者设置、原生依赖发行审查仍是正式公开发行前的独立门槛，不因本地测试通过而自动发布。当前套件原PID与App进程均已不存在，没有等待中的长测。

## 2026-09-23 华为真实开场声学诊断（当前）

新增仅测试包的 `AcousticPromptDiagnosticTest`：原RobotRuntime实际唤醒、CameraX与AudioTrack、原AudioInput/关键词回调持续执行，20轮×6秒；应用音量0.12、系统媒体音量1。只输出固定事件/单调时间/状态，不保存现场录音与画面。实际`:app:assembleDebugAndroidTest`通过，`am instrument -w -r -e class org.familyrobot.app.AcousticPromptDiagnosticTest …`在华为131.081秒正常结束；20/20轮有musicActive采样，麦克风recording/AEC available均为true，配置恢复ACK为true，系统音量finally恢复，结束后force-stop App。音轨活动及AEC可用不证明主观听感或回声消除效果。

**诊断发现：** 第14轮开始3350ms后出现一次本地“停止”事件，原回调照常执行；rest为0。这不是声学零误触发通过，也不能仅凭无录音的事件认定是回声或环境口令。131秒是诊断执行成功，不是产品声学验收通过；此前65分钟长测失败仍有效，没有启动新4小时/24小时测试。

复核发现Android同时加载APK基础词表与服务端自定义词表；此前离线65轮只用服务词表。追加基础词表+generate(小伙伴)自定义流、同0.25阈值的780段直接PCM复测，仍零关键词/零rest（18.975秒）。没有包含手机已下载图书的额外词表、实际环境声与动态回答，因此不能声称完全等同手机词表/声学条件。下一步定位声音来源需要可确认的安静/受控家庭环境；现有信息不足以合理修改口令阈值或屏蔽停止事件。

证据：`acoustic-prompt-build.log`、`acoustic-prompt-device.log`、`acoustic-prompt-diagnostic.json`、`kws-continuous-prompts-app-base.py/.json/.log`；`git diff --check`通过。生产APK仍1ab1，新测试APK1b7de4d7caa90a108e2a79daef94d96685143ea592359b473a43d1df9f37f6fa；此前冻结只代表历史测试工具。OpenSpec8.6/8.7/9.5保持未通过。

## 2026-09-23 连续提示音与观察器复核（当前）

固定Sherpa模型、生产生成词表、与手机相同0.25阈值，将APK内12种原创静态提示音在同一识别流连续输入65轮，共780段，未检测到任何关键词（rest=0）。耗时18.556秒，原始报告 `kws-continuous-prompts.json`、执行脚本 `kws-continuous-prompts.py`，均在私有开发证据目录。实际命令 `PYTHONPATH=ai-service ai-service/.venv/bin/python .artifacts/development/kws-continuous-prompts.py` 退出0。它只排除该组直接PCM输入下的复现，不覆盖扬声器/房间/AEC、环境声音或动态TTS回答；65轮合成回放不能代替65分钟声学或4小时稳定性，旧正式长测失败不改写。

接续观察器已同步配置恢复协议：工作负载complete之后还须有acknowledged=true的configuration-restored，才标记completed-awaiting-analysis；恢复失败、测试失败、时长不足均不通过，旧日志缺ACK不升级为完成。`pytest scripts/tests/test_observe_stability.py -q` 两项通过，覆盖六种事件组合；`ruff check scripts/observe_stability.py scripts/tests/test_observe_stability.py`、`git diff --check`通过。手机App进程为空，未启动新长测。下一步需要真实外放与麦克风链路的受控复核，仍不采集保存家庭原始录音。

## 2026-09-23 21:06 长测配置恢复补齐（当前）

旧正式混合长测3933.4秒失败、24小时未启动，结论不变。已修复测试工具退出时未恢复配置的问题：原配置写入Keystore加密Vault，正常/异常退出经正式API恢复并等待机器人ACK，成功才删除备份；失败保留备份与原异常，残留备份阻止新测试覆盖。最后停止采集。强杀无法执行finally，不宣称自动恢复。

实际验证：`:app:assembleDebugAndroidTest` 构建通过；华为 `StabilityConfigurationTest` 两项15.62秒通过（异常恢复/重建helper、推荐初值恢复）；`scripts/test_stability.py --mode mixed --seconds 60` 退出0，手机60126ms/1循环、instrumentation68.342秒通过，记录 `configuration-restored` 且 `acknowledged=true`。证据 `stability-restore-build.log`、`stability-restore-device.log`、`stability-mixed-20260923-210448/`。这不是4小时或真实声学验收。推荐20:00—09:00、Asia/Shanghai、daily0/media30、计划关闭、override0已恢复并ACK，替代下方旧的“尚需恢复”状态。

生产APK仍1ab1ee35；新测试APK为0d59b3753e5650402959099211c7856722162fe1ec6b0fff1b3acbed542c3ded。旧冻结不再代表新测试工具，未重启正式长测。OpenSpec严格校验、diff空白检查通过；56/61，未勾8.7/9.5，下一步继续排查本地rest事件来源。

## 2026-09-23 安静模式回答修复（当前）

教学用英语问句及紧邻中文释义不再被误删；普通追加邀请仍过滤，空行/标点不占回答句数，清理后为空返回503且不写成功历史。服务端 `pytest ai-service/tests -q` **107项通过，3.60秒**（2条测试库弃用提示），日志 `quiet-reply-tests.log`；Ruff、严格OpenSpec及 `git diff --check` 通过。仅服务端变更，APK未改，不重复Android构建。

真实配对复测各24次：2B文字中位0.75825秒/P95 0.9184秒，4B中位1.3790秒/P95 1.6763秒，双方非空24/24，4B英语问候3/3恢复。当前继续保留双模型的原因是速度差距，原空回答已修复，细节及版本边界见[单模型合并评估](单模型合并评估.md)。

当前服务PID12636，日志 `quiet-reply-final-service.log`，预热ready；项目Ollama90171。最终服务的30语音/20视觉性能测试 `quiet-reply-final-model-benchmark.json` 已通过：语音中位数2.3015秒/P95 2.644秒，视觉中位数3.767秒/P95 4.258秒，固定内容检查50/50、测试身份撤销成功；包含真实ASR→模型→首句TTS，不含手机拾音/VAD/物理出声。华为混合短测已通过：`stability-mixed-20260923-194330/`，实际120192ms、两轮，关键词事件0，PSS131134→138251KB，32→33℃，thermal=0；短测不能替代2小时声学。冻结 `endurance-frozen/20260923-194543-quiet-reply-final/` 后，19:46启动独立4小时mixed→24小时standby；本轮已在3933.4秒明确失败，套件PID15564与手机测试均已退出，24小时未启动。完成64轮，第65轮“真实ASR对话TTS播放”断言超时；失败快照显示唤醒1484ms后触发本地rest，随后STANDBY。源码确认rest调用stop并取消回答，但不能确定声音来源是环境口令还是误识别；保留声学问题，不修改停止规则强行通过。证据 `stability-mixed-20260923-194656/run.json`、`instrumentation.txt` 和采样；36个冻结文件未变。失败后已确认机器人instrumentation结束，force-stop本项目App，pidof无进程，停止采集。长测修改的禁用时段仍需恢复并核对ACK，恢复前不启动日常使用；不要将停止App写成配置已恢复。13.2完成，8.7/9.5和真实儿童/家庭样书等保持未通过。中间版本性能测试因追加空行修订主动中止（exit130，`quiet-reply-model-benchmark.*`），保留为历史，不计通过。

## 2026-09-23 单模型初次比较（历史）

用户授权先比较单4B，故已主动停止原混合手机测试和接续观察器并保存记录；4小时未完成、24小时未启动。真实配对文字调用各21次：2B中位数0.7483秒/P95 0.9171秒，4B中位数1.2656秒/P95 1.6700秒；只驻留4B节约模型报告内存约2.20 GiB。但4B英语问候3次被安静模式过滤为空，单4B复核也3次复现，当前保留双模型。详见[单模型合并评估](单模型合并评估.md)。先修复该服务过滤问题并复核，再建立最终长测证据；下方19:16接续记录是历史状态。

更新于 2026-09-23，当前 0.1.0-dev。证据保存在本地 `.artifacts/development/`，不提交含设备和运行环境信息的原始日志。下列通过只覆盖明确列出的场景；项目尚未通过全部产品验收。

## 2026-09-23 19:16 原混合测试接续观察（历史状态）

18:57套件的宿主在1048.3秒结束，`run.json`和`endurance-suite.json`保留failed；instrumentation输出没有最终成功或失败断言。实际华为仍是原`StabilityTest`进程PID16618、原开始时间，已继续写入第19轮采样；没有重新安装、停止手机测试或重新计时。不能把宿主无终态直接认定为产品崩溃。

只读观察器`observe_stability.py`扩展支持原混合14400秒和待机86400秒，按实际start事件选择采样文件及完整时长门槛。`ruff check scripts/observe_stability.py`及AST语法检查通过，全部冻结版本哈希仍一致；未修改生产、测试APK、模型或服务。接续进程PID804，状态`endurance-observer.json`，证据目录`stability-mixed-20260923-185701/observer-20260923-191557/`。

观察器确认USB转发缺失并恢复，事件记录`usb-forward-restored`；家庭服务90225和Ollama90171仍存活、health ready。手机采样出现过“服务暂不可用”，这段网络中断影响保留，不能称全程无中断。原宿主failed不改写；接续观察只有得到同一起点的完整时长、通过终态、指纹一致及资源趋势后才能评价结果。**原自动套件已停止，24小时待机尚未开始**；待本次混合结果分析完成后再继续后续阶段。

补充[家庭实体验收执行表](家庭实体验收执行表.md)，只提供真人/实体书/实物/家庭网络的步骤与空白记录，不包含新通过结果；新增导航的8个本地链接检查有效。

## 2026-09-23 18:57 最终视觉版本冻结与长测（历史进度）

- 当前生产APK仍为`1ab1ee358f65b1e1f41dfb9aa3ab8d8a29962a60a8555a916e74f5d139f145fb`，测试APK`1ed0fa838eff0549c26b2160277900547b801f378f9eee8f499c42e84b3c2d7d`；服务97项、核心35项、构建/Lint、语音入口和视觉链路证据见下。模型为2B文本/4B视觉、MLX ASR、Qwen3-TTS 1.7B。
- `scripts/test_stability.py --mode mixed --seconds 120` 当前版本通过：**120436ms、2循环**，真实ASR→LLM→TTS首轮响应3172ms，实际媒体播放；手机PSS159087→118353KB、温度33→34℃、thermal=1，退出相机已释放。证据`stability-mixed-20260923-185339/`。合成语音输入，麦克风真实持续开启；记录1次`camera_off`本地事件，不能仅凭计数判定来自环境口令或误检，不记作零误触发或儿童声学通过。
- `scripts/benchmark_models.py --live` 30语音/20视觉通过：语音中位数2.387秒/P95 2.811秒，视觉中位数4.507秒/P95 5.45秒；固定词义/颜色50/50符合，测试身份已撤销。具体范围和冷启动记录见下；仍不等于手机到实际出声的完整产品延迟。
- **18:57:01已启动** `caffeinate -i ai-service/.venv/bin/python scripts/endurance_suite.py --serial <华为序号> --adb <当前ADB> --stages mixed standby`，套件PID92644、混合子进程92646。4小时混合通过后自动接续24小时待机，任一步失败停止后续。冻结副本`endurance-frozen/20260923-185701-visual4b-final/`，实际状态在`endurance-suite.json`；不能按开始时间推算通过。
- 此阶段不改变APK、服务源码、模型锁或运行模型。仍省略`session-timing`，真实安静退出和既有声学失败没有被短测覆盖；完整4/24小时与资源趋势尚未得到结果。8.3按基础设备/模型链路回归完成，11.5按原始逐项核对与本批修订收尾完成；8.5—8.8/9.5仍未完成，OpenSpec未归档。
- OpenAPI与当前路由一致、OpenSpec严格校验通过；源码清单236文件。所选个人路径/设备号/私钥头检查未命中，89个本地链接有效（随后只增加README中的既有有效文档入口）；不替代独立安全/许可审查，不发布APK或仓库。

## 2026-09-23 18:50 视觉模型拆分与华为真实链路（历史进度）

- 采用视觉`qwen3.5:4b`、文本`qwen3.5:2b`，两者的清单、配置与全部模型层经 `scripts/verify_llm.py` 校验通过。4B最初识别勺子正确但只回答用途，追问缺少明确对象；已给JSON字段补充职责，并由服务明确输出观察/一般知识、向文字追问传递对象名。修订后同素材颜色3.335秒、用途4.106秒、双对象3.648秒；黑图0.005秒规则拒绝。首次加载25.886秒单列，已在启动预热加入原创小图，不能把预热视为永久免冷启动。
- `pytest ai-service/tests -q` **97项通过**、2条弃用提示、6.23秒；Ruff与OpenSpec严格校验通过。新增检查确认只安装2B时不谎报视觉就绪，以及真正向4B预热请求附图。日志`visual-model-split-tests.log`、`visual-model-split-ruff.log`、`visual-model-split-hashes.log`。公开OpenAPI与当前路由核对一致。
- 生产APK仍为`1ab1ee358f65b1e1f41dfb9aa3ab8d8a29962a60a8555a916e74f5d139f145fb`。语音入口 `OriginalVoiceRequestsTest` **49.947秒通过**（目录`huawei-OriginalVoiceRequestsTest-20260923-184215/`，测试APK00af），真实服务路由/TTS/媒体、仅转写注入。
- 新增 `VisualModelPipelineTest`：仅重建测试APK **13秒通过**；测试APK`1ed0fa838eff0549c26b2160277900547b801f378f9eee8f499c42e84b3c2d7d`，华为**32.038秒通过**（`huawei-VisualModelPipelineTest-20260923-184404/`）。真实CameraX新帧91/114ms，网络发送前替换成原创红圆/近黑图，不发送现场图像；真实TLS→VLM/文字追问→Qwen TTS→AudioTrack。红圆与无图追问均回答红色，近黑请求拒绝猜测，三轮实际播音和音频字节均有记录。不是儿童ASR、实体物品或AEC证据。
- 用户中断时测试已经完成，但宿主服务/Ollama进程随后退出；18:48重新核查真实进程后恢复项目服务及模型并完成预热，没有把中断误算测试失败或重复安装。当前家庭服务PID90225、项目Ollama PID90171，日志`visual-final-service.log`、`visual-final-ollama.log`；它们独立运行，未操作另一个IPv6 HTTP预览服务。
- 第12.1/12.2修订核对完成。`scripts/benchmark_models.py --live --output .artifacts/development/visual-final-model-benchmark.json` 已完成30轮语音/20轮视觉：语音中位数2.387秒/P95 2.811秒；视觉中位数4.507秒/P95 5.45秒；固定词义/颜色检查50/50符合、测试身份已撤销。此为实际服务ASR→LLM/VLM→首句TTS，不含手机拾音/VAD、家庭Wi-Fi和物理出声；不代表开放问答准确率。当前正在跑华为真实开麦mixed120秒短测，**尚无正式长测**，8.3/8.7/9.5/11.5仍未完成；此前声学失败及家庭/实物验收缺口保留。

## 2026-09-23 18:30 视觉边界与真实模型比较（历史进度）

- `ai-service/.venv/bin/python -m pytest ai-service/tests -q`：**95 passed**、2条测试库弃用提示、4.17秒；`ruff check ai-service/robot_service ai-service/tests scripts`通过。日志`visual-final-service-tests.log`、`visual-final-ruff.log`。覆盖结构化观察、用途/消歧、黑图前置拒绝、未确定对象的代词追问和新图失败清除旧参照；“讲这本书/讲《书名》”复用书库路由，泛化“讲个故事”不变。
- JDK17/SDK36下 `android-app/gradlew -p android-app assembleDebug assembleDebugAndroidTest lintDebug core:test`：**9秒通过**，核心35项，日志`visual-final-android-build.log`。新增英语声音/故事指代排除后生产APK为`1ab1ee358f65b1e1f41dfb9aa3ab8d8a29962a60a8555a916e74f5d139f145fb`；测试APK为`00af70676c9414efca206fd99ac5533cb8d126917528f2d4440ac3d125b91f6c`。
- `scripts/test_huawei.py --class-name VisualRequestPrivacyTest`：本构建**23.6秒通过**，目录`huawei-VisualRequestPrivacyTest-20260923-182603/`。实际CameraX新帧及请求附图检查；转写/回复注入、麦克风隔离/音量0，测试恢复现场配置与音量。先前ba32构建同类24.015秒通过保留为历史证据，不能混称同一APK。
- 真实2B比较使用四张相同原创示意图：颜色2.192秒、勺子用途2.215秒、双图形2.226秒、黑图前置拒绝0.005秒。**勺子仍被拆成“勺子/手柄”而错误要求消歧**；其余双物体/黑图及其代词追问按预期拒绝猜测。证据`visual-purpose/candidate-qwen3.5-2b.json`与`visual-candidate-2b.log`，不把返回200计作语义通过。
- 4B候选下载中；当前正式模型锁/服务仍使用2B。候选比较仅在独立临时数据库、同一图片和JSON结构下覆写带图请求模型。下载后核对清单/各层哈希与真实输出，再决定是否采用；没有预先宣称参数更大即合格。
- [逐项验收矩阵](逐项验收矩阵.md)覆盖原始R/A条目及P0/P1/13补充；[视觉实现说明](视觉请求与对象追问实现说明.md)记录取帧、追问与失效规则。服务进程仍加载17:47的旧代码，待模型选择收敛后正常重启并跑受影响链路。第12.1尚未闭合，**当前无正式长测**，8.3/11.5继续未勾选；旧声学失败、家庭样书/实物视觉及长测缺口均保留。

## 2026-09-23 17:53 原始视觉偏差修复（历史进度）

17:47启动的长测已主动中止，记录6循环、最后采样301507ms，不计4小时通过；取回手机残留记录后仅停止测试App。原因是逐项复核发现R07/R09偏差：原关键词匹配会为“不要记这个”等普通指代上传画面，视觉提示又把回答限制为物体/颜色的一句，不能完整说明一般用途。对应OpenSpec第12节，8.3/11.5重新待核对。不是更换测试条件来掩盖原声学失败。

修订为明确视觉请求才附新帧，过去对象的代词追问不重新取图；模型按问题说明观察及一般用途，多对象/模糊时澄清，保留必要疑问句，不对药品、食物安全和身份作视觉判断。正在执行实际App请求边界和真实本地VLM验证。生产APK已变化，前述4071通过只能作为历史证据；当前不运行正式长测。

## 2026-09-23 17:48 冻结与独立稳定性阶段（历史批次）

- 生产APK仍为`4071d4b5ac25c9b96e5ba9d2d3ede75eb89612e6bd7dac86071c5a2e03e9e5b1`；增加口令时间诊断后的测试APK为`0277b69d26078c21ddacf466bebc1521722ee0d518756445861960270542511a`。服务已加载“别看了”别名，PID60100。基线服务91项、别名变更后定向14项通过；Android核心31项及构建/Lint通过，生产代码没有再修改。
- `scripts/test_stability.py --mode mixed --seconds 120` 第二轮完整通过：**120376ms、2循环**，真实ASR→LLM→Qwen首轮响应2741ms，第二轮实际媒体播放，0关键词事件；PSS135845→139014KB，电池温度35℃、thermal=1。证据`stability-mixed-20260923-174353/`。ASR输入是项目原创合成WAV，麦克风持续实际工作；不是儿童发音或AEC验收。前一轮81秒失败仍未确定原因，不因本次通过而删除。
- 双身份各10次冷启动及OS权限恢复在相同生产APK通过，运行命令与定向回归见下。OpenSpec `openspec validate build-family-robot --strict` 通过。源码检查226个Git可见文件、48份Markdown、81个本地链接、176个归档成员：所选个人路径/设备序号/私钥模式无命中，本地链接有效；这不代替完整安全或许可证审查。
- **17:47:33开始** `ai-service/.venv/bin/python scripts/endurance_suite.py --serial <华为序号> --adb <当前ADB> --stages mixed standby`：4小时混合通过后自动24小时待机，任何失败停止后续。阶段状态在`endurance-suite.json`，套件PID62565；当前目录`stability-mixed-20260923-174733/`。冻结副本在`endurance-frozen/20260923-174733-final-functional-freeze/`，运行期间不改服务/构建/模型锁，也不并发占用手机。
- 本次**明确省略session-timing**：当前真实安静窗口仍未通过，旧版5组真实30秒/10分钟记录不能自动覆盖本版。混合/待机是可独立执行的稳定性测试，不能替代安静退出、儿童声学或家庭样书。第8.7只有完整时长与资源趋势分析完成后才能勾选；第8.6、8.8、9.5仍待完成。
- 当前基线为原始01—13及已批准补充。另一任务新增的V5/V6交互稿核对是并行设计快照，不把其新增历史聊天/音质档位/机器人改名入口默认为本批已实施需求，也不覆盖该任务文件。

## 2026-09-23 17:34 当前构建与定向回归（历史进度）

- 当前生产APK SHA256：`4071d4b5ac25c9b96e5ba9d2d3ede75eb89612e6bd7dac86071c5a2e03e9e5b1`；当前测试APK：`f34377daf3e24294f1185daaee50a79e239f17975829bee16858e5c8f1e28d8d`。生产相对e57仅补入自检取消后的设备所有权保护，以及诊断结果中文说明；服务仍为91项通过的同一版本，未修改服务或迁移数据库。
- `android-app/gradlew -p android-app assembleDebug assembleDebugAndroidTest lintDebug core:test` 在JDK17/SDK36下15秒通过（`final-ownership-build.log`），核心31项。最终仅测试夹具修订后 `assembleDebugAndroidTest` 1秒通过（`final-request-fixture-build.log`）。
- 华为实际命令为 `ai-service/.venv/bin/python scripts/test_huawei.py --serial <华为序号> --adb <当前ADB> --class-name <类名>`。4071构建：DiagnosticHardwareTest **33.977秒**、SettingsApplicationTest **20.163秒**、MediaCompletionTest **90.156秒**、OriginalVoiceRequestsTest **49.409秒**、CameraFeedbackTest **18.032秒**、CameraUnavailableTest **5.413秒**通过。各次`run.json`保存真实APK指纹，不将测试APK更新前后的结果混为一次运行。
- 自检新增真实取消竞态：延迟ASR响应2200ms，在取消后立即重新唤醒，确认旧请求返回后新会话仍持有唯一麦克风。原实现的迟到`finally`可能再次释放已恢复的设备，现由运行所有权保护；已在本构建验证。灵敏度测试使用真实KWS流，确认会话内标准词表不变、结束后应用低/高词表，停止词不变，配置最终恢复。
- e57统一回归已完成的范围：图书阅读 **199.46秒**、图书录入UI **52.517秒**、基础设备 **117.85秒**、音频时限 **115.672秒**，均通过；完整目录见`final-detail-regression.json`。这些历史结果用于未变模块的回归依据，不能声称在4071又运行了一遍。
- 两次测试夹具失败保留：17:17媒体延迟用例87.382秒时没有命中仅最终偏移的延迟条件，不能证明已注入阻塞；改为限定本轮资源、真实进度达到1秒即阻塞，并先断言阻塞发生，17:23两项完整通过。17:24语音入口65.145秒失败时，测试根据滞后的UI快照漏掉了媒体期间的唤醒，没有发出`/turns`；改为在主线程读取真实Session并验证可接受输入，17:30完整通过。这两处只修改测试，不修改产品行为或降低验收标准。
- 当前构建的 `scripts/test_cold_start.py` 双身份各10次冷启动、`scripts/test_permissions.py` 系统权限撤销/恢复已通过。`scripts/test_stability.py --mode mixed --seconds 120` 在81034ms失败：第二轮实际媒体播出断言未满足，失败现场keywordAction=rest且状态STANDBY；当时未记录口令发生时间，不能确认rest是否与媒体失败重合或构成原因。第一轮原创WAV注入真实ASR→LLM→Qwen链路3363ms；麦克风始终真实开启，不能据此称孩子语音识别通过。证据`stability-mixed-20260923-173524/`，状态`final-platform-regression.json`；正式4小时/24小时暂不启动，8.3/8.7继续未完成。下方17:08及更早段落均为历史记录，不能覆盖本节的当前版本与状态。
- 真实儿童、家庭样书与人工听感仍未完成；16:50开麦安静等待失败及90秒无播音VAD观测继续保留，控制测试通过不代表声学验收通过。

## 2026-09-23 17:08 原始细节收尾与统一回归（历史批次）

- 原始第11节已补齐按书名/歌曲/换一个、单项记忆撤回、脱敏诊断和本机有界自检、实际帧驱动相机提示音。追加的11.6包含相机不可用口头说明、数字页码跳号提示、协议兼容拒绝、昵称灵敏度和表演强度。实现与维护规则见[说明](诊断与记忆撤回实现说明.md)。
- 服务：`ai-service/.venv/bin/pytest -q ai-service/tests` **91项通过、2条测试库弃用警告、3.07秒**，日志`final-detail-all-tests.log`。第一次全量90通过/1失败是旧交互配置断言未包含新增默认字段，补充兼容默认值断言后全量通过；不隐去失败记录。
- 构建：JDK17/SDK36下 `android-app/gradlew -p android-app assembleDebug assembleDebugAndroidTest lintDebug core:test` **13秒通过**；补充测试后 `assembleDebugAndroidTest core:test` **2秒通过**。Ruff通过，OpenAPI已重新导出。模型实测：三档昵称逐词阈值被已安装Sherpa解析，1秒数字静音不触发；这不是儿童灵敏度成功率测试。
- 当前生产APK `e57eab7a0f356d182e17981853005b80ac641c9c4341aae95b5007bd70b62b68`，测试APK `f4b41fd7ba2dbf9f5de90808f895dc125bdcc5cca291ed0c875e73f2709e8ffb`。服务已重启加载收尾代码，PID42797，`final-detail-service.log`。本轮源码未最终冻结，长测未启动。
- e13c350b历史合并版：OriginalVoiceRequestsTest **57.46秒**（真实API/TTS/媒体，ASR转写注入）；CameraFeedbackTest **17.936秒**（实际帧与短音轨）；DiagnosticHardwareTest **20.09秒**（48000采样、新帧、ASR及真实音轨，后台和远程静音取消）。这些通过不自动覆盖后续APK。
- e57eab7a：CameraUnavailableTest **6.018秒**，真实撤销OS相机权限后提示、保持相机关闭；先确认真实麦克风可用，再隔离环境语音验证提示。协议2模拟响应下拒绝业务写请求，不冒充实际服务升级。权限已恢复。OriginalVoiceRequestsTest **46.201秒**在统一回归中通过。
- `WakeFeedbackTest` 16:50在e13c350b上**失败**：真实30秒等待期间仍为LISTENING，keyword=none。17:03独立无播音诊断在e57eab7a采样 **90148ms**，记录6个VAD分段、798次语音活动回调、0次关键词、0音频错误；无媒体播放，观察到媒体音量0，结束麦克风已释放。未保存音频/原文。说明无机器人播音时仍存在VAD活动，不能单凭计数判定环境确有语音或模型误报；不能计为安静等待/AEC验收通过。
- 当前顺序执行`final-detail-regression.json`列出的华为入口、相机、诊断、图书阅读/导入、设备、时限和媒体回归。只有各次日志的真实终态有效；任何失败停止后续。当前手机由native5038持有，其他连接设备未操作。


## 当前图书补齐批次（2026-09-23）

原始需求复核遗漏已补入源码，见[实现说明](图书录入与阅读实现说明.md)。本轮服务、规则和华为录入界面检查通过；当前APK阅读保护回归已通过，OpenSpec第10节已按证据完成；72418343构建的8.3基础链路已通过；全功能收尾复核又确认原始需求缺口，第11节补入任务。15:59长测于312秒主动中止，当前不在运行；新路由变更后8.3、10.6、10.8重新待最终构建验证。

- 服务全量：`ai-service/.venv/bin/python -m pytest ai-service/tests -q`，78项通过、2条测试库弃用提示，3.45秒。日志 `book-final-service-tests.log`。包含schema1→2、单页旋转/重拍/迟到保护、处理中取消后拒绝迟到结果及新作业恢复、PDF局部失败与整批超时部分保留、目录边界、不可读/ISBN优先和独立节选音频。新取消测试首次使用错误响应字段名失败，改为实际API字段`draft_version`后通过，未改产品逻辑。
- 核心规则：`:core:test`，31项、0失败，新增4项覆盖明确阅读指令、印刷页码与分段、无页码顺序说明、重复页码章节澄清。
- 构建：`:app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug :core:test`，10秒通过，Lint 0错误/15警告，日志 `book-choice-failure-guard-build.log`。生产APK SHA256 `72418343046b2b5e6b62c4df7cd099e5247d6063fafb1d50fd39beee18cd95d3`。随后仅修改测试夹具，`:app:assembleDebugAndroidTest` 6秒通过；图书回归测试APK `8c509b6e66deebd6158bd6381ef830156dc020dbfccbfd1cdff00bb03039811f`。
- `ruff check ai-service/robot_service ai-service/tests scripts` 与 `openspec validate build-family-robot --strict`通过。
- 服务已重启至新代码，数据库schema2；迁移前私有数据库备份在 `.artifacts/development/pre-page-migration/`。之后为冻结长测再次正常重启家庭服务，`/health`预热ready；当前进程日志 `final-book-freeze-service.log`。

华为实际执行 `ai-service/.venv/bin/python scripts/test_huawei.py --serial <华为序号> --adb <当前ADB> --class-name <用例>`；下列时间为instrumentation自身时间，宿主安装/收集耗时另见各目录`run.json`。

| 运行 | 结果与边界 |
| --- | --- |
| BookImport UI 15:25:37 | 当前生产APK通过，42.774秒：连续拍摄2页、多选2页、真实上传/OCR、顺序/来源、已有审核页保留、单页重新识别、出版社字段；截图和记录在`huawei-BookImportUiTest-20260923-152537/` |
| BookWorkflow 15:38:00 | 当前生产APK通过，207.664秒，测试APK8c509b6e…：上述同版/定位/候选/撤回/真实断网流程，以及提示未说完拒绝继续、注入TTS 503后清除候选。证据`huawei-BookWorkflowTest-20260923-153800/` |
| BookWorkflow 15:10:50 | 前一生产APK `f27c1f21…` 217.026秒通过：实际Qwen音频、AudioTrack、同版续读、明确章节定位、版本变化拒绝、候选取消/过期/选择/撤回、实际USB断网节选与缺说明拒播；新增保护逻辑仍需在当前APK复测 |
| BookWorkflow 15:28:39 | 当前APK宿主189.045秒时ADB连接归属切换，宿主失联而手机测试仍运行；收回手机记录，最终USB映射握手超时。恢复映射供清理，不计通过 |
| BookWorkflow 15:34:01 | 86.754秒失败：上轮断连残留的同名测试书令候选从2本变为3本。已给每轮夹具增加唯一检索别名，真实同名消歧规则不变；15:38整套通过 |
| BookWorkflow 14:45:45 | 三段完整音频下载/哈希通过，首次说明后正文断言失败；缺少状态采样，原因未定，不删除失败记录 |
| BookWorkflow 14:48:35、14:52:56 | 末尾反射控制夹具报`IOException Canceled`。夹具未先进入THINKING/取消旧Job，提示音未结束就推进；已修夹具，不将这两轮标为通过 |
| BookWorkflow 15:04:22 | 开麦克风时在续读断言前进入REST，输入/唤醒可能干扰，未保存原始录音，不能确定声学原因。随后控制测试明确隔离麦克风，正式声学验收仍未通过 |
| BookImport UI 14:58:22 | 测试源码编译失败后误使用旧测试APK，缺少新测试类；无效运行，不用于判定功能 |
| BookImport UI 14:59—15:22 | 多轮寻找动态表单按钮/字段失败，后续发生StaleObjectException。改为真实无障碍滚动并等待文本及位置稳定后，15:25整套通过；失败截图保留 |

相机/选图返回原创夹具，只验证URI、界面和真实OCR入库，不算实体家庭书拍摄准确率。阅读控制由测试入口注入；这类控制测试明确关闭本实例麦克风并将媒体音量设0，最终恢复音量，验证真实音频生成与播放位置，不算儿童ASR、AEC或人工听感。候选超时使用注入过期时间，不冒充实际等待30秒。

ADB归属曾在5038与5037间变化，每次读取设备清单并明确华为序号，不操作同连接上的三星，不杀默认ADB。蓝牙已开启并连接耳机，不能沿用早先“蓝牙关闭/音量0”的环境描述。

### 同生产APK基础回归（15:42起）

- `scripts/test_huawei.py --class-name DeviceSmokeTest`：126.736秒通过，证据`huawei-DeviceSmokeTest-20260923-154249/`，当前生产APK72418343、测试APK8c509b6e。
- `AudioPolicyTest` 首轮15:45:04宿主13.638秒时丢失设备连接，华为随后由原生5038持有；未取得instrumentation终态或结果文件，不计通过，也不凭此认定产品崩溃。确认该测试进程结束后恢复转发，再运行。
- `scripts/test_huawei.py --class-name AudioPolicyTest`：原生后端重跑164.615秒通过，证据`huawei-AudioPolicyTest-20260923-154611/`；20次双击79—138ms、MP3/PCM嘴形、失焦暂停、禁用/静音释放硬件、相机撤权、真实分钟计划边界和59754ms清单限时。
- `MediaCompletionTest` 15:49:03用例组96.608秒：普通衔接通过，慢进度上报场景在自然结束前收到本地`rest`，转STANDBY但保留当前清单位置，结束断言失败。没有原始录音，不能确定环境人声/误检，也不能归因于进度同步阻塞。保留`huawei-MediaCompletionTest-20260923-154903/`两份采样。为定向检验媒体与同步，新的测试夹具关闭本实例麦克风并静音输出，结束恢复音量和身份；声学及正式长测仍保持原范围。测试APK变为`2ff75c2e13d3b8d147d6d982dedd772772265eefd8aeca07efebf99f5e884ffb`，仅`:app:assembleDebugAndroidTest` 2秒通过，生产APK不变。15:52:33重验两项均通过，89.622秒，证据`huawei-MediaCompletionTest-20260923-155233/`；真实播放衔接/结束、延迟上报时的持久最终位置和释放后的服务端同步通过。
- `scripts/test_stability.py --mode mixed --seconds 120`：冻结后服务预热ready，当前生产APK和测试APK2ff75c2e，真实120524ms、2循环、1次ASR→LLM→TTS及1次媒体通过，instrumentation127.057秒；手机PSS153105→142953KB，两次34°C/thermal0。证据`stability-mixed-20260923-155514/`。麦克风真实开启，不等同目标儿童/完整4小时验收。
- `scripts/test_permissions.py`：同生产APK系统相机撤权后保留麦克风、双撤权后不采集、恢复后待机仅麦克风三项冷启动检查通过，并恢复原权限。当前结果`permission-safety.json`，命令日志`final-book-permissions.log`；不等于OS重启。OpenSpec8.3基础硬件/真实模型链路已据本组和上述回归完成，8.5/8.6/8.7/8.8仍待验收。
- 本批源码检查：41份Markdown的71个本地链接有效；OpenAPI与`create_app`临时数据目录导出的契约相等；运动安全核心以`c++ -std=c++17 -Wall -Wextra -Werror`编译执行通过，shell语法检查通过。209个Git可见文件的有限隐私模式检查未发现个人路径、两台测试设备序号、私钥头或常见访问密钥；不代表穷尽敏感信息审查或放行公共二进制。

### 当前冻结长测（2026-09-23 15:59:51）

实际命令：`caffeinate -i ai-service/.venv/bin/python scripts/endurance_suite.py --serial <华为序号> --adb .artifacts/development/adb-native --stages mixed standby`。本轮mixed在宿主312.0秒主动中止，未启动standby。中止依据是原PRD语音点播路由和管理/隐私反馈缺口，非本轮产品崩溃；向已核实的test_stability子进程发送SIGINT，脚本停止手机测试，之后确认宿主与ActiveInstrumentation均结束。冻结副本`endurance-frozen/20260923-155951-book-complete/`，生产APK72418343、测试APK2ff75c2e，家庭服务已正常重启加载当前源码、预热ready。运行情况以`endurance-suite.json`和真实进程为准，不能按开始时间推算通过。

状态明确`requestedStages=[mixed,standby]`、`omittedStages=[session-timing]`、`fullSuiteRequested=false`。此前会话用例缺少完整安静窗口，仍属于未通过；不关闭麦克风或放宽门槛冒充完成。正式长测保持真实麦克风/本地KWS，控制测试中的输入隔离不用于本组。USB归属当前为原生5038，默认5037与另一台设备不操作。长测期间不重建APK、不重启服务、不改变生产代码；只有说明文档及证据可以更新。8.7、9.5与最终归档尚未完成。

### 收尾语音路由补齐（长测停止后）

原始路由实测：`读《小兔子》`、`请读小兔子`、`唱首歌`、`换一个`进入chat；`这本书里的小兔为什么伤心`进入book。证据`remaining-intent-audit.json`。已补明确阅读请求与普通提问的区分，书名点播复用同名/未发布/缺正文处理，允许无相机点播；儿歌请求只选择已发布实际音频；换一个携带当前资源上下文，排除当前项和下架项。Android已接入book_result并发送currentResourceId。

`pytest ai-service/tests -q` 82项通过、2条弃用提示、3.34秒；新增4项覆盖明确/否定/普通提问、无相机书名/未发布/歧义、换一个及下架、儿歌必须已审核有原音频。Ruff通过。Android `:app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug :core:test` 12秒通过，日志`media-request-build.log`。新服务代码尚未重启至设备，新APK未安装进行本轮手机验证，不拿旧72418343证据覆盖。OpenSpec第11节其余缺口继续实施，所有功能复核闭合前不重启长测。

## 历史基线与演进记录

以下保留各次开发时的证据，不代表这些旧APK通过记录覆盖本轮新构建。

| 检查 | 实际命令 / 方式 | 当时结果 |
| --- | --- | --- |
| OpenSpec | `openspec validate build-family-robot --strict` | 最新严格校验通过，实施未归档 |
| 服务与 API | `ai-service/.venv/bin/python -m pytest ai-service/tests -q` | 34 项通过；配对/角色/配置冲突、导入版本、撤回恢复、播放清单、记忆编辑/删除中断、模型超时、MLX 静音前置过滤、儿歌实际音频与图片解码前限额、容量/恢复回滚、后台合成不阻塞实时语音；2条测试库弃用提示 |
| Android 核心 | `android-app/gradlew -p android-app :core:test` | 17 项通过；会话、跨日额度、迟到开场回调、计划初启/错过不补播、10分钟问答结束不误停独立媒体 |
| Android 构建与检查 | `android-app/gradlew -p android-app :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug` | 最近一次通过，含分句播放、管理页生命周期、下载配额互斥和 WAV 分块解析修订 |
| 运动安全核心 | `c++ -std=c++17 -Wall -Wextra -Werror -Imotion-firmware/include motion-firmware/tests/safety_test.cpp -o /tmp/family-robot-safety-test` 后执行 | 最新编译执行通过；仅主机安全逻辑，无物理运动硬件 |
| 华为基础及离线 | `ai-service/.venv/bin/python scripts/test_huawei.py`（指定本机 adb-native 包装脚本、serial） | 最新133.252秒通过（含唤醒诊断及立即停止旧音轨修订）：TLS正误指纹、登记/PIN、纯脸、麦克风与新相机帧、完整下载与进度、强制心跳仍在线时的断网回退、联网撤回、正确AppOps确认管理/后台停止录音、家长各标签切换；断网双击93ms |
| 华为会话真实计时 | `scripts/test_session_timing.py`（由长测套件调用） | instrumentation 3203.357秒通过：20次请求后新帧、20次相机退出、5组真实30秒等待、5组真实10分钟到期及休息提示；续问事件注入，不计儿童声学 |
| 华为音频与计划 | 真机 `AudioPolicyTest` instrumentation | 最新152.616秒通过（立即停止旧音轨修订后的构建）：20次双击停止90–124ms（从第二次触碰开始计，含触碰处理），MP3 解码、PCM 嘴形、焦点丢失暂停且不自动恢复、静音/禁用/撤销相机释放硬件、真实分钟边界启动清单、整份清单限时 59,982ms；不等同家庭声学验收 |
| 真实模型与书库 | `ai-service/.venv/bin/python scripts/model_smoke.py` | 最新通过：原创混合 PDF 真 OCR、封面识别入库匹配、英语试听、原文分段音频、中英合成语音回读、静音不产出文字、VLM 红圆识别及下架拒绝 |
| 模型完整性 | `ai-service/.venv/bin/python scripts/verify_llm.py` | 此前固定 Qwen 清单及所有层 SHA256 通过；下载权重逐个校验，未将未完整下载文件视为可用 |
| 有限命令 KWS | `ai-service/.venv/bin/python scripts/kws_smoke.py` | 本机真实引擎+原创合成语音 7/7；不等同手机外放回声或儿童成功率 |
| 稳定性脚本冒烟 | `ai-service/.venv/bin/python scripts/test_stability.py --mode mixed --seconds 120`（指定 adb、serial） | 最新120.342秒真机通过，包含一次真实模型回答（2.945秒）和一次媒体播放；PSS 147000→136153KB，温度33°C，thermal=0。仅验证长测脚本，不能替代4小时或24小时 |

## 模型性能与上下文

`scripts/benchmark_models.py` 使用真实本地 ASR→LLM/VLM→首句 TTS，30次普通问答和20次合成视觉输入。CPU small 方案中位数4.334秒未达目标；MLX最初重复编码、缺少VAD和问候语言偏离也分别失败，保留原始比较记录。加入VAD、复用单次音频编码和短回答后，一轮实测普通问答中位数2.364秒、P95 2.958秒，视觉中位数2.304秒、P95 2.499秒，内容检查50/50通过。最终语言与上下文规则修订后再次完整通过：普通问答中位数2.595秒/P95 3.418秒，视觉中位数2.561秒/P95 3.185秒，内容检查50/50通过。

这些数据不包含手机拾音/VAD、网络和输出缓冲；内容关键词检查不能替代人工语义评价，也不能代表目标儿童识别率。首次安装导入依赖及冷加载一度达到72秒，已提供启动预热，不能把冷启动掩盖为热启动速度。

`ai-service/.venv/bin/python scripts/conversation_smoke.py`：真实本地模型10组×5轮文本上下文测试通过。此前出现擅自把中文名字翻译为英文、添加未提及喜好以及混淆位置，已限制语言、沿用用户最新事实并采用确定性解码。它验证上下文，不是儿童语音5轮验收。

权限：`scripts/test_permissions.py` 真机通过撤销相机后冷启动、同时撤销录音后冷启动、恢复后仅待机麦克风三项；使用已验证格式的 `cmd appops get`，不沿用旧 `dumpsys appops` 的无效结果。

热降级：华为 `ThermalSafetyTest` 8.089秒通过，框架SEVERE注入后关闭实际麦克风/相机/媒体，reset后不自动续播。已核对 `IsStatusOverride: false`，未把框架注入当作真实散热试验。

## 已修复的关键回归

- 播放完成后才开始30秒追问；迟到开场回调不能覆盖媒体状态；取消后的 ASR/LLM 不能重新出声。
- CameraX 取得真实新帧；后台及管理页返回不误开麦克风；单 Activity 避免重复音频管线。
- 下载遇模型忙有界重试；断网刚发生、心跳仍在线时可回退完整同版本缓存；HTTP撤回/凭据与TLS错误不回退；容量检查在写入锁内复核。
- 家长标签清空旧数据并拒绝迟到响应，修复摘要页误读设备数据崩溃；UI测试等待横向滚动停止，避免点击移动中的坐标。
- 对话10分钟到期关闭问答与相机，媒体仍保留有效播放代际并遵循独立30分钟上限。
- 旧音频焦点回调不能暂停新播放器；MP3 使用 MediaCodec→PCM 音轨驱动嘴形。
- 播放清单在加载前进入会话；计划时长覆盖整份清单，启动/错过不补播。
- OCR 取消重试创建新作业，防迟到写入；撤回保留当前有效版本；图片像素限额在解码前执行。
- 记忆删除/修改使正在生成的旧回答失效；长期记录只使用家长已批准且未过期内容。

## 待取得的验收证据

时段/额度组合：`PolicyBoundaryTest` 与 `scripts/test_policy_boundaries.py` 已在旧长测结束后运行。首轮 `policy-boundary-20260923-110614/` 未在预算内耗尽额度，配置恢复成功；第二轮 `policy-boundary-20260923-111255/` 实际分钟禁用停止488ms、离线拒绝唤醒、解禁不自动续播、额度耗尽与离线进程重启保留额度均通过，配置恢复且主APK未变。但第二轮逐秒采样发现首条23.677秒音轨结束后仍处SPEAKING且继续计费，不能据用例通过认定清单组合交付完成；正在增加音轨/队列/协程诊断定位。系统重启与异常时钟不在该用例范围内。

2026-09-23补充宿主音轨准备检查：使用与上述脚本相同的PyAV编码流程，将项目原创 `unclear.wav` 重复8次编码并重新解码，得到95181字节、24000Hz单声道、23.677秒MP3；结果保存在 `policy-boundary-audio-precheck.json`。只验证测试素材可生成与解码，不代表手机播放、时段或额度已经通过。

真实家庭样书至少10本、清晰封面30次及歧义20次、全文中英文人工核听；目标儿童30次唤醒、5轮连续对话10组、播放中口令停止与2小时背景声；24小时待机完整记录及分析（4小时混合已完成，见下）；完整权限撤销/重启/异常时间组合。单台手机顺序切换身份不代表两台手机同时使用。没有证据的项目保留未通过。


## 当前无人值守验收

2026-09-23 01:25（Asia/Shanghai）启动 `scripts/endurance_suite.py`，本轮在217.7秒时主动中止：逐条审计发现原PRD的30秒发言边界和1.2秒断句未完整落实，需要修正后重新建立证据。此次不计正式4小时通过。首轮真实模型回答2.542秒、温度33°C、thermal=0，仅为首轮样本，尚不能判定长期通过。原构建指纹与中止记录保存在 `endurance-history/20260923-012544-manual-stop`，修正后会重新固定指纹。

2026-09-23 02:02（Asia/Shanghai）在最新整包回归及120.342秒冒烟通过后，重新启动 `scripts/endurance_suite.py`。本轮先运行 `SessionTimingTest`：20次请求后新帧与释放、5组真实30秒等待、5组真实10分钟会话；通过后自动进入4小时混合、24小时待机。有效续问由测试事件注入，不能计入儿童声学验收。

阶段状态在本地 `.artifacts/development/endurance-suite.json`，当前计时记录在 `.artifacts/development/session-timing-20260923-020205/`。本轮已固定服务源码、模型清单和APK指纹。RSS是进程驻留内存观测，不等于Apple统一内存/GPU的全部分配；最终需结合趋势与设备实际行为分析。三阶段结束与分析完成前，8.7继续保持未勾选。

开源整理检查：44个本地Markdown文档链接有效；历史v1.2 ZIP中个人机器绝对路径已替换，ZIP完整性通过；APK打包保留合并的AL2.0/LGPL2.1许可证资源。尚未公开发布，也未把这些检查声称为完整二进制许可审计。


### 发言边界补充（2026-09-23）

- 服务/API新增完整30秒WAV接受、30.1秒和声明长度不完整拒绝测试；34项通过。
- 核心新增临近等待截止时已开始发言、播放声不得更新发言、10分钟到期本地提示可取消且4秒硬上限；17项通过。
- 华为 `SpeechDetectorTest` 0.527秒通过，使用实际原生Silero VAD和原创合成音频验证纯音、句末静音及超过20秒的长段；不是实时儿童录音验收。
- 28.1725秒原创合成英文经真实MLX识别用时3.162秒，末尾“purple kite”保留；证据 `asr-30-second-boundary.json`。
- 相机实例增加代际检查，视觉请求等待请求之后的帧；20次真实采集与释放加入下一轮计时验收。

双身份冷启动：`scripts/test_cold_start.py` 已实际终止/重启进程各10次，共20次通过；家长页无持续录音、机器人页仅本地唤醒入口。此后唤醒首句缓存与待机亮度调整的整包回归另行记录。

纯脸横竖屏：华为 `FaceRenderingTest` 15.119秒通过，12种表情在两种方向共24张真实组件截图，断言方向尺寸与无文字；人工抽看横屏哭泣、竖屏休息布局无裁切。证据 `face-gallery/`，不代表孩子能理解全部表情。最终整包 `DeviceSmokeTest` 115.028秒通过。

最新计时阶段中间证据：20次视觉请求均取得请求之后的新帧，20次退出均释放相机；5组真实30秒等待全部完成，30036—30282ms。5组真实10分钟到期分别为600049ms、600143ms、600160ms、600062ms、600197ms，实际休息提示播放、相机关闭及回待机断言通过。原始日志包含20个新帧事件、20个释放事件、5个等待事件、5个会话到期事件和complete，instrumentation为 `OK (1 test)`；会话计时子项已完成。

源码清单：`ai-service/.venv/bin/python scripts/source_manifest.py` 新增组合测试后重新生成146个Git可见文件的SHA256，HEAD为空、dirty=true，未提交或发布。临时仓库验证已忽略数据不纳入、未提交源码纳入、符号链接拒绝；当前清单未包含runtime、.artifacts、.tools、AAR或密钥路径。此清单不代替逐文件隐私与许可审查。

2026-09-23 02:55:38（Asia/Shanghai）计时阶段通过后，套件自动进入正式4小时混合测试，目录 `stability-mixed-20260923-025538`。已确认子进程和真实首轮问答：长时间待机后首轮7050ms，首个手机样本PSS 139072KB、电池温度35°C、thermal=1，退出后camera=false。这是开始样本，不是4小时结果，也不能据单轮推断中位延迟；服务及APK固定指纹仍一致。

混合测试前10分钟中间观测（非最终验收）：实际采样至602434ms，11个手机样本，PSS 137313—141961KB、温度35°C、thermal=1、相机残留样本0；6次问答首轮7050ms，后续2343—2513ms。此时服务进程及长测进程仍存活，无failed事件；尚不足以证明4小时稳定或持续内存趋势。

混合测试半小时节点中间观测：采样至1867433ms（约31.1分钟），32个循环、16次问答，中位数2449.5ms；首轮后范围2273—2563ms。手机PSS 137313—147547KB，最后142818KB，温度33°C、thermal=0，相机残留样本0、failed事件0。主机最后一次采样服务RSS612448KB、Ollama RSS2901904KB；保留 `observation-30-minutes.json`。仍不代表完整4小时/24小时通过。

混合测试一小时节点中间观测：3614940ms、61个循环、31次问答，中位数2457ms，nearest-rank P95为2766ms，首轮7050ms；不含拾音/VAD。手机PSS范围120248—147547KB，前/后15个样本中位数139522/123945KB，末温度32°C、thermal=0，相机残留样本0、failed事件0。服务、长测及ADB进程均存活，全部冻结指纹一致；详见 `observation-one-hour.json`。仍保留4小时/24小时验收未通过。

混合测试两小时节点中间观测：7288089ms、122个循环、61次问答，中位数2456ms、nearest-rank P95为2648ms；不含拾音/VAD。手机PSS范围109439—147547KB，前/后15样本中位数139522/114963KB；末温度32°C、thermal=0，相机残留样本0、failed事件0。主机末次服务RSS611472KB、Ollama RSS2902064KB；相关进程存活，全部冻结指纹一致。证据 `observation-two-hours.json`，仍不计完整4小时/24小时通过。

### 正式4小时混合结果与24小时接续

2026-09-23 06:56（Asia/Shanghai），`scripts/endurance_suite.py` 调用的 `scripts/test_stability.py --mode mixed` 正式阶段通过。真机计时14402249ms，instrumentation为 `Time: 14,408.889`、`OK (1 test)`，宿主完整运行14419.4秒；240个循环包含120次真实ASR→LLM→TTS与120次媒体播放，无failed事件，240个采样均在线且退出后相机关闭。阶段前后服务源码、模型清单和两个APK指纹一致。

120次问答从提交原创WAV至实际播放的中位数2447ms，nearest-rank P95为2648ms，首轮7050ms、最小2233ms；该路径不包含物理拾音/VAD。手机PSS范围99648—147547KB，末值105423KB；各小时中位数139787.5、122407.5、111939、103971.5KB。服务RSS各小时中位数617432、614496、615480、619136KB，Ollama为2905264、2904016、2903576、2905456KB，本轮未观察到持续累积增长。电池温度35°C降至31°C；28个样本thermal=1，后续212个为0，没有严重热状态。真实高温下的效果仍不能由这轮正常温度运行替代；热降级的框架注入证据另列。RSS同样不代表全部GPU/统一内存。

完整原始日志和统计位于 `.artifacts/development/stability-mixed-20260923-025538/`，分析文件为 `observation-final.json`。套件于06:56:06自动启动 `stability-standby-20260923-065606` 的86400秒阶段，已核对真实instrumentation子进程、首轮对话2576ms及待机采样。24小时阶段尚未完成，8.7仍未勾选；此前各中间节点仅保留为历史过程记录。

### 待机观察连接中断（2026-09-23 10:14）

宿主instrumentation连接意外结束，原驱动运行11889.0秒后将待机阶段记为failed，末次本地手机采样11852755ms，未出现failed/complete。现场确认ADB服务于10:14:31重新启动，USB反向转发为空；具体是谁触发重启尚无证据。手机原PID 26833仍存活，`ActiveInstrumentation` 为未结束，私有采样继续推进，因此未重启、安装或终止手机测试，不能将宿主退出声称为应用崩溃。诊断保存在该轮 `termination-diagnostics/`；其中系统日志包含历史及其他应用事件，不能混入本轮失败统计，也不应公开提交。

恢复 `adb reverse tcp:8765 tcp:8765` 后，真实采样重新显示online=true。新增 `scripts/observe_stability.py` 接续同一开始事件与PID，持有长测锁并保留新的手机/主机采样及固定指纹；原suite失败结果和日志不改写。第一次观察器为修正连接查询的错误分类而退出并重新启动，仅更换宿主观察器，手机PID及计时不变。实际执行 `ruff check`、`ruff format --check` 均通过，正式接续命令见维护手册；它尚未完成24小时观察，不能作为完成验收。后续跟进首先查看 `endurance-observer.json` 及活进程，不能看到原suite为failed就重复启动长测。

接续核对：恢复后的手机日志完整保留原日志前缀；截至第208个采样，出现2个offline样本，仍保持本地麦克风开启、相机关闭，全部采样最大间隔66721ms（包含正常周期问答所需时间）。这不等于精确网络中断时长，主机RSS的观察缺口另行保留。10:26完成恢复后的第15个周期对话，实际ASR→LLM→TTS播放3074ms，随后回到在线待机，无failed事件。阶段性分析位于该轮 `connection-interruption.json`，24小时整体仍待完成。

### 待机明确失败及诊断修订（2026-09-23 10:38）

接续观察在13296095ms取得手机明确failed：`AssertionError: 待机不启用相机`。前一分钟相机关闭，失败采样相机开启；手机PID 26833随后因instrumentation结束退出，观察器状态为device-failed。本轮24小时未通过，不能再作为运行中的测试。原套件状态、两份原APK及指纹保存在 `endurance-history/20260923-020204-standby-failed/`，手机完整记录和退出信息在该轮 `observer-20260923-101951/`。

源代码审查发现旧长测把两次计划问答之间都视为待机，但实际本地KWS始终工作；原采样不含会话状态与触发来源，所以本轮无法区分外部唤醒与状态/相机异常，也不能断言本次一定是误报。新增仅内存的唤醒来源、时间、有限口令类别和唤醒计数，不保存原始录音、文本或书名；没有调整KWS阈值。长测改为在主线程取得一致状态，检查实际STANDBY下无相机/媒体残留，并检查超过30秒无新输入时相机关闭，保存失败状态及实际KWS计数。非计划唤醒必须另审，不等同背景声误唤醒验收通过。

`android-app/gradlew -p android-app :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug` 33秒通过；随后完善失败快照不可用时仍保留原错误，单独重建测试APK通过。当前正在重新执行华为回归及待运行的时段/额度组合；主APK已经变化，因此旧版4小时结果只作历史证据，新版本需重新执行正式长测。

新版首轮DeviceSmoke在62.529秒因双击计时421ms失败，保留 `wake-diagnostics-smoke-failure/`。该用例用UiDevice.click返回后的时间衡量停止，包含同步等待；改为与AudioPolicyTest一致，从第二次触屏事件注入开始轮询真实音频停止，保持300ms目标，尚需重跑证明。下一轮宿主ADB再次断开，手机测试框架收尾发生 `UiAutomation.disconnect` / `DeadObjectException`，保留 `wake-diagnostics-smoke-crash/`，不记通过。

进一步找到10:14、10:35、10:49的macOS ADB崩溃报告，均含SIGABRT及 `LibusbConnection::write_cb` / `free_tiny_botch`，提取记录在 `adb-usb-crash-evidence.json`。默认5037重启还与其他自动启动客户端发生监听竞争。确认测试均已终止后，将此测试手机detach并转入5038独立原生USB后端，设备重新可见、host-features无libusb；未修改系统SDK。当前使用 `.artifacts/development/adb-native` 包装脚本重新回归；不能仅据换后端就声称长期稳定问题已解决。

原生USB后的真实触控轮次仍测到392ms，因此没有将421ms简单判为工具误差。再加分段计时，测得总366ms、暂停回调到达42ms、暂停函数3ms、最近落盘19ms，延迟出现在回调完成后的音频结束观测。取消路径由仅pause/flush改为保存实际进度后立即静音、pause/flush/stop旧音轨，后台协程仍负责释放，续播创建新音轨；避免等待IO清理才stop。修订后完整DeviceSmoke 133.252秒通过，停止93ms（回调42ms、暂停执行4ms）；断网续播进度及联网撤回同轮通过。原失败分别保留在 `wake-diagnostics-smoke-native-failure/`、`stop-timing-failure/`，通过日志为 `stop-track-huawei.log` 和 `device-smoke.txt`。随后AudioPolicyTest 152.616秒通过，20次实际触控停止90–124ms。另新增MediaCompletionTest，普通两条MP3顺序播放与自然结束51.814秒通过；这不能排除禁用/离线重连之后的组合播放故障。

2026-09-23 11:26的第三轮时段/额度组合：实际禁用停止286ms、离线拒绝及解禁不自动播正常；第二条音轨播放头确实推进，但约46秒触发本地 `rest`，提前停止，额度未耗尽，用例失败、配置恢复。不能以直接PCM负样本未触发KWS推断手机声学不会误触发。

进度阻塞定向检查：原代码在每段播完后同步等待进度PUT。`MediaCompletionTest#slowProgressReportDoesNotBlockPlayback` 在测试客户端阻塞进度回报，真实首条结束后采到无音轨、SPEAKING、队列尚有一项，35秒内不能衔接而失败（`media-progress-stall-before.txt/jsonl`）。已改为加密持久待同步队列、单发送者与5秒网络调用限时；每资源合并最新位置，最多128项并有超限诊断。修订后的早期用例有段间静默误作自然结束的断言漏洞，不作为通过证据；已加强第二音轨播放头推进、清单真正清空、最终位置上传检查。后续轮次仍有本地wake/rest中断或测试退出，保留日志，尚未完成此修订的最终验证。

11:40左右系统音频日志显示多次A2DP连接/断开及蓝牙触发静音。已在私有 `test-device-environment.json` 保存原蓝牙开启状态，暂时关闭本测试手机蓝牙，用内置扬声器重测；这不证明此前所有失败都由蓝牙造成，也不等同AEC验收。全部手机测试结束后必须恢复蓝牙原状态。

2026-09-23 11:43后发现同仓库有用户另行启动的唤醒/触摸反馈及Qwen TTS开发任务，同时修改RobotRuntime、AudioInput、MainActivity和服务端。已协调停止本任务的手机安装/长测，保留并行需求改动；唤醒任务已合并本任务暂停原因诊断与新反馈逻辑。后续以合并后的源码重新构建验收。11:33之后的非冻结构建与手机环境交互不能作为最终固定版本证据；关键词中断也不应在未控制环境时直接归因为自身回声。进度等待的代码缺陷和定向注入结果保留，但改后完整回归仍待统一进行。

开源打包检查：`ruff format/check scripts/dependency_inventory.py` 通过，基础 `.venv` 实际清点68个Python发行包，6个缺完整随包许可，其中本项目自身已补 `license-files`；其余5项为CTranslate2、flatbuffers、mlx-whisper、sherpa-onnx-core、tokenizers，需按具体发行物继续核对，不能将基础清单用于新增TTS环境。`pip wheel --no-deps --no-build-isolation ./ai-service --wheel-dir .artifacts/package-audit` 成功，实际解包验证自有MIT、NOTICE及词表Apache-2.0全文3份文件均在wheel，MIT/Apache文件与仓库原件字节一致。记录为 `package-audit/verification.json`；此临时wheel仅验证打包，不是功能验收后的公开发行包。

新TTS依赖环境独立清点：`.venv-tts/bin/python scripts/dependency_inventory.py --output .artifacts/dependency-audit-tts` 成功，当前36个发行包，1个（tokenizers）缺完整随包许可文件。mlx-audio0.5.5的MIT全文已从实际发行包提取并与上游v0.5.5许可核对。AAR的arm64 C API经 `xcrun llvm-objdump --dynamic-syms` 可见OfflineTts导出函数，证明该包并非只暴露KWS；不据此直接推断具体GPL链接结论。对应私有清点和符号文件在 `dependency-audit*/`。

并行功能合并等待期间，主任务没有继续占用华为手机。已恢复原蓝牙开启状态并实际核对 `bluetooth_on=1`，私有环境记录 `currentlyAltered=false`；原生ADB5038仍保留供后续统一测试。后续若再次临时调整蓝牙，须重新记录并在该轮测试结束恢复，不能沿用旧“仍关闭”的状态描述。

合并验收脚本补强（尚待新版真机运行）：MediaCompletion按素材实际时长判定两条自然完成，正常/延迟采样分文件；test_huawei新增该测试入口、排他占用保护、每轮独立目录、失败采样和APK变化拒绝。Ruff通过，实际持锁执行“不可用ADB”负例在任何手机操作前退出，证据 `huawei-driver-lock-check.json`。新TTS锁文件纳入长测指纹，宿主样本增加TTS PID及其内存子集。新需求的30秒短结束音使CLOSING与STANDBY不再同一时刻，SessionTimingTest仍要求退出边界在30,000—30,500ms、当时相机关闭，另外要求1秒内结束尾声并停播；旧版5组30秒数据不作为新行为验收。


## 2026-09-23 Qwen合并回归与用户体验交接

主APK `4381db3fe525b297db2fc16fd64e760e43589c5ae469154087edd3f82f6259cd`，家庭服务已显式使用 `qwen3-mlx` 并预热。新TTS和触摸任务已完成其定向验证，完整记录分别见《Qwen3-TTS接入验证》《唤醒与触摸验证》。不能沿用旧Kokoro模型性能证明新版性能。

实际执行的可复现写法：`ai-service/.venv/bin/python scripts/test_huawei.py --serial "$ROBOT_SERIAL" --adb "$ROBOT_ROOT/.artifacts/development/adb-native" --class-name DeviceSmokeTest`。`ROBOT_SERIAL` 为 `adb devices` 显示的目标设备，`ROBOT_ROOT` 为本仓库绝对路径；实际序列号及原命令仅保留在私有证据中：

- 第一轮 `huawei-DeviceSmokeTest-20260923-122451`，JUnit 19.826秒失败于进入家长管理。新入口要求2秒长按，旧测试使用150个swipe步骤约750ms。测试改为实际注入DOWN、保持2150ms、UP，两处入口同步更新；未改生产手势阈值。
- 修正后 `:app:assembleDebugAndroidTest --offline` 成功（1秒）；同步消除MediaCompletion诊断字段的nullable调用警告。测试APK SHA256为 `9bbdf3f851c83e0e7a5325d36aaed6136a8b8b0b8a441baf936051ca35fee0b9`。
- 第二轮 `huawei-DeviceSmokeTest-20260923-122625`，用户通过Qwen任务要求立即安装新版体验，主任务在36.500秒主动中断。`run.json`保留 `passed=false` / `KeyboardInterrupt`，不是完整产品失败或通过。没有启动后续AudioPolicy、MediaCompletion、真实时段／额度、长测。
- 脚本finally已结束instrumentation并恢复USB反向端口8765；实际检查无残留测试进程、蓝牙开启（1）。手机安装／打开交由用户体验任务接管；本任务不再操作手机、不重启服务、不运行模型压力负载。自动跟进已暂停，不会定时打断体验。未擅自恢复禁用时段，以免中断刚请求的体验；最终家庭安全配置移交仍待正式测试结束执行。
- `ruff check scripts/test_huawei.py scripts/endurance_suite.py scripts/test_stability.py scripts/dependency_inventory.py`：All checks passed；`openspec validate build-family-robot --strict`：valid。

恢复工作顺序：用户结束体验后，先核对当前源码、服务、APK指纹与实际占用，再顺序完成新版华为回归和性能冒烟，全部短测通过后建立新的真实会话计时、4小时混合、24小时待机证据。未完成前不归档OpenSpec，不将旧版通过或模拟器证据冒充新版本华为验收。

体验交接补充：安装任务随后回报最新版assembleDebug成功，主APK仍为上述4381db3f指纹；华为 `adb install -r` 返回Success，MainActivity启动Status ok、截图确认机器人脸部，TLS健康预热ready，反向端口与蓝牙保持原状态。该记录来源于安装任务的实际验证，本任务未再次操作用户手机。

用户随后明确要求恢复自动跟进，原自动化已恢复ACTIVE。核实无其他手机测试、服务仍运行、主APK4381db3f与测试APK9bbdf3f未变后，重新启动DeviceSmoke；不沿用主动中断结果。公开验证文档中的本机个人路径及设备序列号已改为环境变量写法，原始证据保留私有目录。补齐tokenizers 0.23.2、CTranslate2 4.8.2、flatbuffers 25.12.19对应上游版本的完整许可及SHA256，见THIRD_PARTY_NOTICES；不改变原安装包清点结果，不等同公共二进制审查完成。


## 2026-09-23 恢复后的合并回归

生产主APK保持 `4381db3fe525b297db2fc16fd64e760e43589c5ae469154087edd3f82f6259cd`；测试APK因补强用例分轮构建，具体指纹见各轮run.json。下表命令均使用 `test_huawei.py --serial "$ROBOT_SERIAL" --adb "$ROBOT_ROOT/.artifacts/development/adb-native" --class-name <类名>`，未改生产手势或KWS阈值。

| 类名 | 结果与实际范围 | 私有证据目录 |
| --- | --- | --- |
| DeviceSmokeTest | 122.469秒通过；双身份/配对、完整缓存下载、断网播放/进度、撤回/后台；双击104ms | huawei-DeviceSmokeTest-20260923-123228 |
| MediaCompletionTest | 89.261秒，2项通过；正常与进度响应延迟下，两条实际MP3播放头推进、自然结束和最终进度同步 | huawei-MediaCompletionTest-20260923-123459 |
| AudioPolicyTest | 145.466秒通过；20次双击75—134ms、焦点/静音/禁用、相机撤销、实际分钟计划及59850ms上限 | huawei-AudioPolicyTest-20260923-123731 |
| ProgressOutboxTest | 7.693秒通过；旧ACK不删新位置、IOException重试、Keystore持久位置与序号、已清空队列后的序号不回退、128项上限 | huawei-ProgressOutboxTest-20260923-124045 |
| TouchInteractionTest | 11.966秒通过；Compose真实触摸互斥与9张状态截图 | huawei-TouchInteractionTest-20260923-124100 |
| WakeFeedbackTest | 40.268秒通过；开场嘴形／倾听、真实30秒退出、取消、仅表情、系统音量0、静音与后台 | huawei-WakeFeedbackTest-20260923-124535 |

ProgressOutbox采用测试HTTP拦截应答和运行对象重建，不是OS重启或真实断网；实际断网播放另由DeviceSmoke覆盖。所有AudioTrack、嘴形、系统音乐活动断言都不等于物理扬声器声学验收。WakeFeedback首轮宿主USB短暂断连、无完整JUnit结果；确认目标测试进程已结束后复跑，第二轮失败时系统扬声器媒体音量为0。测试改为保存原系统音量、临时3、最后恢复，并增加零音量必须保持的断言。第三轮通过；蓝牙暂时关闭固定内置扬声器，原值和待恢复状态记在test-device-environment.json，最终须恢复开启。

PolicyBoundary新增真实多音轨推进及超过3秒无播放的失败保护，避免“静默卡住但仍计费”被算作额度通过。新组合第一次失败于配置ACK，恢复也失败；只读网络诊断记录连续heartbeat422。原因是App新增progress-overflow/rejected/response后，服务FailureCounts仍拒绝这些字段。已补三个有界别名、存储保持公开字段名并同步OpenAPI；新回归覆盖故障计数后心跳/家长诊断、旧空心跳、未知字段和越界拒绝。更新服务后，policy-boundary-20260923-125636的restore-only通过，随后重新执行完整组合。旧失败记录均保留；只恢复成功不能算完整时段/额度通过。

另按原P0要求补齐单模型TTS优先调度：当前段结束后对话优先，同级FIFO，总等待8项/后台4项，对话最多等10秒、后台1秒，取消/超时不泄漏名额。不会中断正在执行的推理，不复制大模型。`pytest ai-service/tests -q` **62 passed，2条测试依赖弃用提示，1.77秒**；Ruff全服务/测试/scripts通过，公开契约重新导出。新服务明确使用qwen3-mlx及mlx ASR，预热ready。

`python scripts/tts_concurrency_smoke.py` 实际向新服务请求原创音频：热短句1.447秒、当前后台段5.519秒、并发短答6.232秒、额外后台429/1.010秒，期间心跳200/0.014秒。测试身份用完已撤销。记录在tts-concurrency/results.json；外部请求的到达顺序不能替代确定性调度单测，这次通过也不等于所有产品延迟目标通过。新的30语音/20视觉基准使用 `benchmark_models.py --live --output .artifacts/development/qwen-model-benchmark.json`，直接复用已运行模型，避免另加载一份模型干扰内存；结果待完成后分析。


修复后完整PolicyBoundary在 `policy-boundary-20260923-125826` 通过：真实禁用边界262ms停止，断网拒绝唤醒、解禁不自动播放；108139ms剩余额度阶段6条音轨播放头实际推进，110335ms（含1.5秒落盘等待）记录耗尽；离线终止并重开进程仍为Permission.QUOTA，配置最终恢复。该轮heartbeat没有422；不含系统重启或异常改时钟。这里“音轨推进”依据AudioTrack，不作为物理声学结论。

新版实际服务模型基准完成，50轮均匹配各题的预设关键词（只作该合成集检查，不代表开放问答正确率）：语音30次中位2.6885s、P95 4.064s；视觉20次中位2.5055s、P95 3.195s。满足基准使用的语音中位≤4s、视觉中位≤6s/P95≤12s；临时测试身份已撤销。结果 `qwen-model-benchmark.json`，脚本新增--live使用实际运行服务，不另载一份模型。手机完整端到端及新正式长测仍需后续运行。

120秒新版本端到端冒烟 `stability-mixed-20260923-130346` 通过：实际120306ms、2循环（真实ASR对话与资源播放）、首答2665ms；退出相机/音频释放，KWS非计划唤醒0，热状态0，33℃。仅为短测。2026-09-23 13:07固定两份APK及32项服务/模型/脚本指纹后，正式endurance_suite已启动，依次真实会话计时、4小时混合和24小时待机。固定副本在endurance-frozen/20260923-qwen-1306；测试APK指纹39849f3e815d2ba6c0d29ce11aebaec2872ad60f93832baad88a73c8f19d9042，主APK仍4381db3f。启动与进程存在只证明正在运行，不等于阶段通过。

13:07正式套件在83.583秒失败：20次新相机帧和释放完成，首轮30秒无输入退出未在32秒观察窗内出现。4小时和24小时均未启动，套件状态failed；记录保留endurance-history/20260923-130706-idle-failed及原固定APK目录。原用例缺少输入时间／会话状态快照，当前不能区分计时问题与实际输入重置。已补每秒有限状态、输入时间／代际诊断，不记录语音原文；新增--focus idle定向运行20次相机+1次真实30秒，明确diagnosticOnly，不算完整会话计时或长测。测试APK重建与脚本Ruff检查通过；定向结果尚待取得。


### 30秒失败后的定向诊断与重新计时（2026-09-23 13:18）

`test_session_timing.py --focus idle` 完成：`session-idle-diagnostic-20260923-131211/run.json` 为 passed=true、diagnosticOnly=true，实际30秒边界30050ms、短结束音后待机30459ms；20次请求后新帧与释放也通过。每秒记录显示该轮没有关键词唤醒、输入时间或代际变化。此结果不能解释13:07原失败，也不把原失败改为通过。诊断只增加测试观测，未修改生产计时逻辑或关闭KWS。

13:18重新启动完整顺序套件，先5组真实30秒与5组真实10分钟，再4小时混合、24小时待机。固定副本 `endurance-frozen/20260923-131804-diagnostic`，主APK保持4381db3fe525b297db2fc16fd64e760e43589c5ae469154087edd3f82f6259cd；测试APK为5725fa48037b2b89571973005828f2e53a1e0d3b52f7a2cd8b435d092261001b。状态及源指纹以 `endurance-suite.json` 为准，启动不能计通过。若再次失败，优先检查新样本中的lastInputAt/followUpAt/generation，再区分真实输入、异常状态与计时偏差，不能用放宽阈值或禁用拾音消除失败。

### 资源换声证据核对

服务测试 `test_published_voice_stays_fixed_across_engine_switch`（含旧记录缺失ttsProfile的情况）与 `test_changed_model_revision_cannot_fill_previous_cache` 已覆盖固定发布快照、旧缓存字节不覆盖、缺段409、重发产生新revision及模型commit变化拒绝补段。它们使用受控合成返回值，证明版本契约，不证明实际音色质量。当前华为DeviceSmoke只证明新资源完整下载、哈希、断网播放和下架清理，尚未证明旧/新两版换声过程；OpenSpec 9.2继续未勾选。需在手机空闲后补充：下载旧版并记录哈希、只改全局声音仍原版、离线保持原版、显式重发后当前阅读仍固定旧版、在线重新点播从新版开头读、新版完整下载后断网播放，并核对revision与音频哈希；同时验证下载中断不把partial当成完整资源。新增测试若不改变生产APK，应分别记录测试APK和同一生产APK指纹，不能把测试代码更新冒充产品行为改变。

原始技术方案§资源版本规则复核：普通重新发布不同于下架，不能用撤回通知打断当前旧版阅读。已纠正文档中“普通重发使旧缓存立即失效”的错误描述，验收按固定当前快照、下次在线新版起点、显式完整下载切换离线版本进行。


### 历史撤回与普通改版修复（2026-09-23）

补写换声真机测试时发现：`/catalog/revocations` 只有activeRevision，历史下架墓碑一直存在；客户端把“不等于最新版本”当成已撤销，导致曾下架过的书在后续普通改版时，当前仍可读的旧快照被停止。新增服务复现测试最初失败，保存在resource-revocation-red.log。13:18套件因此主动停止，留档endurance-history/20260923-131818-stopped-for-revision-fix；该轮5次30秒边界30053/30119/30210/30124/30043ms通过，不计完整10分钟组、4h或24h。

修复增加精确revokedRevisionIds；手机只清理实际撤销的版本，整体下架仍清理全部，缺少新字段的旧服务保守兼容。同时补齐在线重新点播新版本时的版本提示和从头读，离线不会为该提示发起合成。此轮修改了生产服务与App，需重新固定最终版本建立长测证据。

实际运行：`python -m pytest ai-service/tests -q` **63 passed**；`gradlew -p android-app :core:test :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug --offline` **BUILD SUCCESSFUL**，核心27项零失败，lint零错误15警告；全量Ruff通过。日志resource-revision-all-service-tests.log、resource-revision-android-build.log。新增ResourceVoiceRevisionTest已编译，正在手机执行；未将编译通过记作真机通过，9.2仍待结果。


ResourceVoiceRevisionTest首轮133126在“真正离线”断言失败：只移除ADB反向端口不足以断开已存在的keep-alive连接，已保留失败，不计断网通过。修正测试连接准备：取消测试客户端在途请求、清空连接池，再以全新客户端请求确认ConnectException。第二轮 `huawei-ResourceVoiceRevisionTest-20260923-133326` **61.222秒通过**，两次真实不可达后在线标志false，旧版/新版播放头分别推进480/3360帧；普通改版跨过3.5秒撤回同步仍播放旧版，在线新版有一次明确版本提示并从0页0ms开始；第二段注入传输失败清理partial且保留旧清单/哈希，新完整下载的两段哈希均改变。测试最终恢复原配置。测试用自动审核标记不代表人工试听，hash改变不等于音色质量验收。

本轮生产APK SHA256 `e162277c2340f9c3bafd83e03b5dab0c50cb56eb947513b75bed05715bea126f`，完整测试APK指纹见该run.json。随后重新运行DeviceSmokeTest与MediaCompletionTest，结果未出前不计通过。


同一e162277c生产APK的受影响回归完成：DeviceSmokeTest **121.227秒通过**（huawei-DeviceSmokeTest-20260923-133513，双击停止101ms，回调42ms、pause5ms）；MediaCompletionTest **89.250秒、2项通过**（huawei-MediaCompletionTest-20260923-133722，真实两条媒体自然衔接及进度响应延迟场景）。两轮APK前后哈希一致，运行脚本退出0。随后启动120秒端到端冒烟，尚未记为完成或正式长测。


新版短时稳定性 `stability-mixed-20260923-133928` 通过：120505ms、2循环，active20197ms，非计划KWS唤醒0，两个退出样本camera=false；PSS152194→141597KB，thermal1，电池34℃。formalDuration=false，不能当4小时通过。

2026-09-23 13:42:56固定新版后启动正式顺序套件，先完整计时再4h/24h。冻结副本endurance-frozen/20260923-134255-resource-revision，生产APK e162277c2340f9c3bafd83e03b5dab0c50cb56eb947513b75bed05715bea126f；测试APK fa8398a82bdc7288577e30316081ef340dce7499295d120bc7c2b0bab9422c4e；32项源指纹另存。当前实际状态以endurance-suite.json和进程/样本为准，启动不计通过。


### 计时验收前提修正（2026-09-23 13:52）

13:42轮在无输入检查中失败；新逐秒样本明确显示输入时间推进、generation45→46，状态依次识别/思考/回答/续听，说明本轮不是连续无输入窗口。原测试从最初起点算32秒即断言退出，与其前提不符。保留endurance-history/20260923-134256-input-during-idle；没有保存或推断环境音内容，不能以此认定13:07原失败同因。

仅修正测试：每组最多180秒/4个窗口，遇输入记录idle-interrupted-by-input，等回答结束后从新的followUpAt重新测量完整30秒；仍要求30秒误差≤500ms，打断窗口不算通过，麦克风/KWS保持开启。10分钟阶段允许正常识别/回答等active状态，但不允许提前退出或唤醒重置起点。驱动总上限由60调整到80分钟以容纳被打断窗口，实际10分钟边界不变，单轮诊断上限5分钟。新测试APK构建及脚本Ruff通过。

13:52:27启动最新正式套件，冻结副本endurance-frozen/20260923-135227-quiet-windows；生产APK仍e162277c，测试APK为5f9857967ab3dfa556af5aee6fe65f09cb3c52bedd4436ef4b93c8575816f251。服务未修改或重启。最新是否通过须看实际过程与终态，4h/24h尚未开始。


### 13:52轮安静窗口未满足与独立长测安排

会话测试在178.646秒停止：两组完整安静窗口30257/30203ms通过，第三组连续四个候选窗口被输入打断，达到次数上限。记录保存在endurance-history/20260923-135227-no-quiet-window；不是完整5组通过，也没有完整10分钟组。系统状态检查显示内置扬声器、STREAM_MUSIC静音且音量0、蓝牙关闭。不能从状态序列判断输入是人声还是噪声，也不能推断13:07同因。

新增MicrophoneEnvironmentDiagnosticTest：只做90秒真实录音/VAD统计，机器人不播放声音、不调用ASR，不保存音频或转写。它输出幅度/时长/事件次数，完成只证明诊断执行，不代表儿童声学或安静环境通过。测试APK编译通过；首次编译的局部lateinit引用错误已修正，生产APK保持e162277c。

为推进互相独立的验收，endurance_suite.py增加可选--stages；默认仍全部按序执行。选择mixed standby时，状态明确包含omittedStages=[session-timing]和fullSuiteRequested=false，不能因长测完成替代被省略的会话验收。乱序参数在访问设备前被拒绝（exit2），Ruff通过。待诊断结束再启动独立长测，不并发占用手机。


90秒无输出诊断完成：huawei-MicrophoneEnvironmentDiagnosticTest-20260923-140056，实际90133ms，7个VAD片段、446次语音活动回调、1次关键词事件、0录音错误。所有采样musicActive=false、系统媒体音量0、AEC启用；结束recording=false。仅保存幅度/时长与计数，没有音频或文字。这排除了本轮由机器人自身播音引起的输入，但不确定是环境人声、噪声或识别误检，不能据此调高儿童唤醒阈值或宣称背景声验收通过。

14:03:19以 `endurance_suite.py --stages mixed standby` 启动独立正式长测。生产APK仍e162277c，测试APK a5685979b4f717e0d3d8a18d5846cff68aad9b8833abb00ad29cb67fa42adad8，冻结副本endurance-frozen/20260923-140318-independent-stability。状态包含requestedStages=[mixed,standby]、omittedStages=[session-timing]、fullSuiteRequested=false；会话计时未完成，4h/24h尚未通过。

维护文档一致性修订：按MainActivity声音页与FaceGestures实际源码更新家长手册的按钮、轻点唤醒、顽皮反馈、资源换声和完整下载规则，并修正工程手册旧“单击不唤醒”说明。需求追踪恢复状态与阅读指南12/13索引同步；只改文档，不改变正在测试的生产构建。


### 原始图书需求逐条复核与补齐（2026-09-23）

逐条核对R11—R26发现原任务拆分遗漏，见《图书原始需求复核》和OpenSpec第10节：批量拍摄/选图、受控目录、单页旋转重识别、OCR质量提示、节选播报、口头选书与续读选择、不可读原因和按页章定位需要补齐。这些属于原需求，不因已有基础测试通过而降低范围。完成该批生产功能后再冻结最终构建跑长测。

14:03长测实际终止情况：宿主322.6秒时失败；检查发现5038没有设备，手机转由默认5037持有。通过5037补取phone-final-samples.jsonl，手机在427690ms、online=false时“真实ASR对话TTS播放”断言失败，随后无App PID或ActiveInstrumentation。原本拟为补功能停止测试，但force-stop实际返回device not found，不能把终止记成主动停止。归档endurance-history/20260923-140319-transport-failed；当前没有长测运行，不计4h或24h通过。

第10节首批服务修改已做：模糊/无文字封面返回RETAKE；已建库未发布、无正文、已下架分别返回NOT_READABLE及原因，未入库仍NOT_FOUND；草稿正文和候选ID不暴露给查询结果。定向 `pytest ai-service/tests/test_book_readability.py ai-service/tests/test_library.py ai-service/tests/test_intelligence.py -q` **22 passed**，对应Ruff通过。手机口头解释尚未接入，10.6未勾；现有服务进程尚未重启，不把磁盘改动当作设备已使用新接口。

### 17:38 声学阻断继续定位

固定提示资源逐一直接送入同版本Sherpa关键词引擎（阈值0.25、当前默认词表），12份原创WAV均未输出关键词，记录`prompt-kws-negative-diagnostic.json`。这只排除这批数字音频输入的直接复现，不能排除空气传播、环境声音或混响。没有保存现场原始录音或识别文字，不能断言rest来自误识别或真实口令。再进行一轮90秒无播音现场统计，以决定当前环境是否适合继续长测；不调门槛、关闭麦克风或吞掉停止命令来让用例通过。

### 17:45 最小口令补齐及失败诊断

- 核对PRD R03原话发现仅有“不要拍了/关闭相机”，漏掉“别看了”。已在本地关键词生成和服务确定性路由补上别名，复用现有camera_off动作，不改停止或灵敏度规则。`pytest ai-service/tests/test_keywords_settings.py ai-service/tests/test_intelligence.py -q` **14项、0.70秒通过**，2条已有弃用提示；相关四文件Ruff通过。真实Sherpa接收原创Kokoro合成“别看了”检出camera_off，属于数字直送模型验证，不是儿童口令验收。
- 家庭服务正常重启加载该修订，PID60100、`final-alias-service.log`，ASR mlx/TTS qwen3-mlx/preload1；不迁移数据。Android生产4071不变。旧91项全量为别名前基线，不能写成修改后重跑91项。
- 第二轮无播音诊断`MicrophoneEnvironmentDiagnosticTest`完整通过采集/释放检查：**90157ms、5个VAD分段、828次语音活动回调、0关键词事件、0音频错误**，媒体音量0、AEC启用；结束实际麦克风释放。其“通过”只表示诊断运行结束，不表示现场安静或VAD准确。
- 为确定媒体失败与KWS事件时序，仅为StabilityTest加上最多20条固定关键词动作/单调时间戳以及失败时播放状态；原关键词回调完整执行，不吞掉停止命令、不录音或记录自由转写。`assembleDebugAndroidTest` **2秒通过**，`stability-diagnostic-build.log`。生产APK不变；测试APK更新，指纹由每轮run记录。正在用相同120秒标准做有诊断的复测，不把上一次失败改成通过。
