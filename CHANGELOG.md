# 更新记录

本轮新增：局域网下载官网与 APK 自动分发——`release_app.sh` 构建后自动发布到 runtime/releases，`run_release_site.sh` 提供 8767 端口只读官网（扫码/浏览器首次安装）；家长端 App 启动自动检查新版本弹窗升级、设置中可手动检查，下载经家庭服务加密通道并校验 SHA256，伙伴端无任何升级提示。详见《局域网官网与APK分发》。其余收尾：按书名读书/换一个/歌曲请求分流；单项偏好撤回与家长处理记录；白名单诊断导出及PIN本机有界自检；真实帧驱动相机提示、相机失败本地语音；明确数字页码跳号/逆序提示；协议兼容校验；仅昵称的三档灵敏度及轻柔表演强度。对应服务测试和分版本真机记录见工程自测记录，儿童声学与最终长测仍待验收。

## 0.1.0（首个正式版）

- 应用更名为「爱陪伴」，安装包标识更换为 `com.robot.app`（旧版 `org.familyrobot.app`）；官网安装包文件名统一为 `<应用名>-v<版本号>.apk`。
- 注意：新旧包名不同，已安装旧版（小伙伴机器人）的手机会并存安装而不是覆盖升级，需先卸载旧版（会清除本机离线内容与配对）再安装「爱陪伴」。
- 首个正式签名发行版（versionCode 2）：`bash scripts/release_app.sh --release "更新说明"` 构建并自动发布到官网与家长端升级通道；签名材料位于 `runtime/android-signing/`（随代码仓库维护，建议同时离线备份）。
- 发布渠道区分 debug / release；官网与家庭服务升级接口均指向最新正式版，官网保留历史版本供首次安装或对照下载。
- 下载官网支持 launchd 托管（`release_site_service.sh install`）：登录自启、异常退出自动拉起，已在 8767 实机验证；不经 bash 以绕过 macOS 对 launchd 进程读取项目目录的隐私限制。
- 注意：正式版签名与开发预览版（0.1.0-dev，debug 签名）不同，已安装开发版的手机会被系统拒绝覆盖安装，需先卸载旧版（清除本机离线内容与配对）再安装正式版。
- 本版按家庭局域网内部分发；发布检查清单中的真机、儿童与长稳验收项仍待完成。

## 0.1.0-dev（开发预览）

开发预览渠道（debug 签名），已被 0.1.0 正式版取代，仅用于本机调试构建。源码提供 Android 单 APK 的家长/机器人身份、家庭本地服务，以及独立运动安全核心。

- 家庭连接：TLS 指纹、短时登记和本机确认配对、家长独立撤销、PIN 与管理员恢复。
- 儿童交互：纯脸、本地唤醒与停止、短轮对话、无预览相机、使用时段/额度、安静模式和英语计划。
- 内容库：图书/故事/儿歌/英语资源、图片与混合 PDF OCR、原稿校对、审核试听发布、封面找书、原文朗读、完整校验下载、离线播放和撤回同步。
- 家长维护：成长与英语分级独立设置、声音选择、最小记忆审核、资源版本、备份恢复、使用摘要与诊断。
- 开发支持：分项目说明、API 契约、模型版本与完整性锁、测试脚本、GitHub Actions、贡献和安全说明。

实际测试结果、失败项和命令见 [自测记录](docs/engineering/自测记录.md)。发布前须完成 [发布检查](docs/engineering/发布检查.md)。已知边界：目标儿童声学、真实家庭书籍、完整4/24小时稳定性尚未全部验收；不支持公网上云默认开放、底盘实机运动或后台锁屏系统级唤醒。调试 APK 没有作为公开二进制完成第三方分发审计。

- 修复家长摘要标签迟到数据、断网瞬间完整缓存回退、10分钟问答时限误停媒体。Qwen版本共用一个TTS进程，当前推理完成后优先处理对话，后台等待有界。
- 增加三题短游戏、OS权限及温控真机检查、连续4小时/24小时自动测试入口；长测与儿童/实体图书验收状态分别记录。

- 按PRD恢复30秒发言和1.2秒句末停顿，端侧使用真实Silero VAD；唤醒首句补齐、临近等待截止续接、10分钟本地结束提示与相机会话代际校验。
- 增加两种身份各10次进程冷启动、真实墙钟计时与20次新帧/释放验收入口；待机降低帧率与窗口亮度。
- 接入可替换的Qwen3-TTS 1.7B本地语音后端、音色与语气设置；已发布资源固定声音版本，旧缓存保留，缺段拒绝混用新声音。
- 唤醒增加可听可见反馈、轻点唤醒与触摸表情；音频播放进度异步持久同步，不阻塞清单自然衔接。
- 图书增加连续拍摄、多图选择、受控目录导入、出版社和质量提示、单页旋转/重拍/重识别；部分提取失败保留已完成页，取消和迟到结果不覆盖校对稿。
- 看封面读书增加两候选口头确认、同版本续读选择、明确页码/章节定位、细分不可读原因；节选范围独立播报并纳入完整离线校验。
- 数据库升级到schema2（增加作业options）。升级前停旧服务并备份；回滚必须恢复匹配的代码和数据库，详见[图书实现说明](docs/engineering/图书录入与阅读实现说明.md)。

- 补齐原需求“别看了”的端侧词表及服务路由别名；修复取消自检后迟到ASR释放新会话设备的竞态。
